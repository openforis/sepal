// TODO: Implement table export to SEPAL (see toSepal.js): export to Drive or Cloud
// Storage, then download to SEPAL. The image-export download steps can largely be
// reused - only the EE task and eventual GDAL processing differ.
export const tableToSepal$ = _options => {
}

Previous reply after the tool result was empty.

If the request still needs action and tools are available, emit exactly one structured tool call now. Do not write XML/function-call text. If no available tool can complete the request, say so plainly — name the missing capability, suggest the closest thing you can do, and stop. Don't fall back to a read-only tool to look busy. Don't return empty content.

version: "3.9"

services:
  ssh-gateway:
    volumes:  # To allow dynamic reloading in dev
      - ${SEPAL_PROJECT_DIR}/lib/js/shared/src:/usr/local/src/sepal/lib/js/shared/src
      - ${SEPAL_PROJECT_DIR}/modules/ssh-gateway/src:/usr/local/src/sepal/modules/ssh-gateway/src

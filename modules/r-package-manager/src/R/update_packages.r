#!/usr/bin/env Rscript
# usage: <URL> <lib>

# args = commandArgs(trailingOnly = TRUE)

# url <- args[1]
# lib <- args[2]

# tools::write_PACKAGES(dir = "/R/cranroot/bin/contrib", type = "source")
tools::write_PACKAGES(dir = "/R/cranroot/bin/contrib")

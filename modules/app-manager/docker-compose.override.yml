version: "3.9"

services:
  app-manager:
    volumes:  # To allow dynamic reloading in dev
      - ${SEPAL_PROJECT_DIR}/lib/js/shared/src:/usr/local/src/sepal/lib/js/shared/src
      - ${SEPAL_PROJECT_DIR}/modules/app-manager/src:/usr/local/src/sepal/modules/app-manager/src

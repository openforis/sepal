{
  "storage-driver": "overlay2",
  "log-driver": "syslog",
  "log-opts": {
      "cache-disabled": "true",
      "syslog-address": "{{ SYSLOG_ADDRESS }}",
      "tag": "docker/{{ '{{.Name}}' }}"
  }
}

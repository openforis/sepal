# progress.md — AI test refactor

Source of truth for resumption. Per-entry verdicts + running coverage delta.

## Baseline

Captured: 2026-05-19, branch `mcp-ports-and-adapters`.

Command: `npm run coverage -- --runInBand` from `modules/ai/`.

| Surface | Stmts | Branch | Funcs | Lines |
|---|---|---|---|---|
| All files | 94.62 | 87.99 | 94.82 | 95.15 |
| src | 77.77 | 86.66 | 75.86 | 76.92 |
| src/chat | 100 | 100 | 100 | 100 |
| src/chat/conversation | 93.13 | 88.81 | 94.89 | 93.46 |
| src/chat/llm | 96.15 | 87.23 | 100 | 100 |
| src/chat/llm/providers | 97.61 | 80.48 | 94.44 | 97.18 |
| src/chat/llmText | 100 | 100 | 100 | 100 |
| src/chat/specialists | 98.16 | 87.05 | 100 | 99.47 |
| src/chat/tools | 97.35 | 93.27 | 96.47 | 98.52 |

Test counts: **59 suites, 601 tests, 5.8 s.**

Files with 0 coverage (re-exports / composition; expected):
- `src/chat/conversation/redisChatStorage.js`
- `src/chat/conversation/userChats.js`
- `src/logListener.js` (partial: 42.3 % lines)

## Inventory

61 `*.test.js` files under `modules/ai/test/`, 9,707 LOC (the initial 9,501 figure missed `test/chat/diagnostics.test.js` at 206 LOC). Two `*.manual.test.js` files are excluded by default — hence 59 suites.

Existing harness/builders in `test/`:
- `test/chat/builders.js`
- `test/chat/conversation/fakeRedis.js`
- `test/chat/conversation/inMemoryConversationsStore.js`
- `test/chat/conversation/inMemoryHistory.js`
- `test/chat/conversation/titleGeneratorHarness.js`
- `test/chat/conversation/userChatHarness.js`
- `test/chat/conversation/wsHandlerHarness.js`
- `test/chat/llm/providerConformance.js`

Phase 1 work consolidates these behind a single `test/chat/harness.js` exposing
`aUserChatHarness`, `aConversationHarness`, `aToolFactoryHarness`,
`aRegistryHarness`, all returning **real domain** + side-effect collectors,
with an in-memory history port-fake supporting `append$` + `load$`.

## Phase ledger

### Phase 1 — Harness consolidation

| Step | Status | Notes |
|---|---|---|
| `aRegistryHarness` | done | Real `createToolRegistry` + recording bus. Knobs: `tools`, `bus`. Collectors: `bus.events`, `invoke`/`invoke$` returning envelopes. |
| `aToolFactoryHarness` | done | Real specialist factory (`consult_map` / `describe_recipe`) + fake inner LLM + default fake inner tools + fake guiRequests. Knobs: `specialist`, `replies`, `innerTools`, `guiRequests`, `bus`, `context`. Collectors: `llm.receivedMessages`/`receivedTools`, `tool`, `bus.events`, `innerTools.invocations`, `guiRequests.requests`, `invoke`/`invoke$`. |
| `aConversationHarness` | done | Real `createConversation` + real `createToolRegistry` wrapped to collect invocations. Knobs: `id`, `initialMessages`, `replies`, `tools`, `bus`, `history`. Collectors: `history.load$`, `bus.events`, `invocations`, `send$`, `conversation`. |
| `aUserChatHarness` | done | Real `createUserChat` + real `createConversations` + real `createMessageHandler` + in-memory `conversationsStore` + per-conversation in-memory histories + fixed clock + silent title generator. Knobs: `conversationIds`, `replies`, `tools`, `initialMessagesById`, `clock`, `bus`. Collectors: `conversationsStore.list$`/`get$`, `historyFor(id).load$`, `channelEvents`, `bus.events`, `invocations`, `handle$`. |

Files added:
- `modules/ai/test/chat/harness.js` — single source of truth for all four builders.
- `modules/ai/test/chat/harness.test.js` — TDD scaffolding (deleted at end of Phase 3).

Consolidations:
- `modules/ai/test/chat/conversation/inMemoryHistory.js` — now a thin re-export of `createInMemoryHistory` from `harness.js`.

Open items:
- `aUserChatHarness` aborts auto-commit on every builder due to user memory rule; commits are staged for the user to run. See terminal report.
- Phase 3 migration of existing tests is out of scope.

**Reviewer verdict: Accept.** Quality bar passes; all 614 tests green (601 baseline + 13 harness). Nits parked for Phase 3:

- `aToolFactoryHarness` exposes `innerTools` / `guiRequests` / `bus` / `context` / `receivedTools` collectors not yet exercised in `harness.test.js`. Accept as Phase-3-enabling surface; let scenario tests pin them as they consume.
- `SPECIALIST_BUILDERS` dispatch table grows linearly as specialists are added; consider a `factory:` knob.
- `readSync` returns `undefined` on a non-synchronous chain — would be a silent miss. Tighten when the first async port shows up.
- `createInMemoryConversationsStore` is now duplicated (`harness.js` vs `test/chat/conversation/inMemoryConversationsStore.js`). Phase-3 cleanup: collapse to the harness copy.

### Phase 2 — Seam-map production

**Reviewer verdict: Accept.** Seam-map covers 61 test files + 9 helpers + 7 scenario plans (77 entries). Risky calls (`conversationToolLoop`, `runSpecialist`, `updateRecipeTool`, `wsHandler.*`) all have behaviour-by-behaviour splits to specific scenario files. Helper deletions are gated on `read` / `readError` / `aFakeGuiRequests` being exposed from a sibling of `harness.js` (or moved into it) — recorded in seam-map.md.

Nits parked for opportunistic Phase 3 cleanup:
- Entries section claims "61 test files" but disk has 62 (`harness.test.js` is Phase-1 scaffolding tracked in Appendix A).
- `builders.js` row uses inline "(no, …)" strikethroughs in the last-consumer list — cosmetic.
- `conversationToolLoop` entry mis-attributes one behaviour to that file when it actually originates from `conversationSpecialists.test.js`.

### Phase 3 — Implementation loop

Entries are tracked in [seam-map.md](./seam-map.md). One row per entry below
once Phase 3 starts.

| Entry | Verdict | Coverage delta | Notes |
|---|---|---|---|
| userChatLifecycle.test.js → conversationLifecycle.test.js | Accept | 95.15 % → 95.15 % lines (no change); src/chat/conversation 93.46 % → 93.46 % | 20 scenarios replace 11 brittle tests. Harness gained `llm` + `conversationsStore` knobs and `anAdvancingClock` helper; `harness.test.js` still green. conversations.js 97.82 % lines, userChat.js 100%. Reviewer Accept (2026-05-19) — no red-flag violations; all 9 seam-map behaviours pinned. Nits parked: rename "stops in-flight stream" → "drops late textDeltas after delete" (optional); lift `firstValue` / `run` helpers into harness when next scenario lands; collapse the duplicate `inMemoryConversationsStore.js`. |
| specialistConsultationTools.test.js → specialistConsultations.test.js (full) | Accept | 95.15 % → 95.15 % lines (no change); src/chat/specialists 99.47 % → 99.47 % | 16 scenarios at the `aToolFactoryHarness` seam + the `aConversationHarness` integration seam. specialistConsultationTools.js 100 % lines; specialistScope.js 100 %; runSpecialist.js 100 % under full suite. Source file deleted. No harness changes. Reviewer Accept (2026-05-19) — all 9 in-scope behaviours pinned; no out-of-scope facets crept in; schema/envelope whole-shape `toEqual` earns its place; prompt content asserted via regex markers. Nits parked: lift `collect`/`firstValue`/`run` helpers into `harness.js` next session (now 3 consumers). |
| conversationSpecialists.test.js → specialistConsultations.test.js (consult_map directAnswer facet) | Accept | n/a | The directAnswer streaming row for consult_map landed as the orchestrator-integration describe block. Source file retained for the update_recipe + describe_recipe rows (→ recipeEdits.test.js) and the negative no-directAnswer row (→ userTurns.test.js). Reviewer Accept (2026-05-19). |
| runSpecialist.test.js → specialistConsultations.test.js (cap-behaviour facet) | Accept | n/a | The user-observable cap consequence (specialist always returns an answer even when inner LLM never stops asking for tools) pinned through `aToolFactoryHarness` with a tool-call-only reply. Source file retained for the stall-recovery facet → recipeEdits.test.js. Reviewer Accept (2026-05-19). |
| conversationToolLoop.test.js → toolFailures.test.js (guard/cap/retry facets) | Accept | 95.15 % → 95.15 % lines (no change); src/chat 100 % → 100 %; src/chat/conversation 93.46 % → 93.46 %; toolCallGuard.js + conversationLoop.js + terminalNotices.js 100 % lines under the focused run | Six facets pinned at the `aConversationHarness` seam: tool-failure envelope + assistant answer, TOOL_REPEAT_BLOCKED short-circuit + does-not-count toward bail, consecutive-failure bail notice, INVALID_TOOL_ARGS bail notice, tool-round cap notice, empty-after-tool retry (single retry + hint + tools-on-retry + tool-call-on-retry). Source file retained for the residual facets routed to `userTurns.test.js` (happy tool-round path + multi-call + unknown-tool envelope + runtime-context retention + channel-emission collision + no-orchestrator-restate for non-directAnswer tools). Reviewer Accept (2026-05-19) — all 9 in-scope behaviours pinned; no out-of-scope crept in; matcher discipline holds. Nits parked: optional decoupling from `MAX_TOOL_ROUNDS` import / hardcoded `max: 3` (mirror conversationToolLoop precedent); trim local `collect`/`firstValue` from `harness.test.js` if it survives Phase 3. |
| updateRecipeTool.test.js → toolFailures.test.js (construction + metadata-failure facets) | Accept | n/a (folded into the row above) | Two facets pinned at the `aToolFactoryHarness` seam: construction-time validation throws when either `load_for_update` or `recipe_patch` is missing from the inner registry (two parametric `it`s); preflight metadata-lookup failure short-circuits with `{ok: false, error: {code: 'TOOL_FAILED', message: 'GUI gone'}}` and never invokes the inner specialist LLM. Source file retained for the bulk of update_recipe behaviour (directAnswer flag, schema, prompt assembly, scope violations, success/failure envelopes, retry-within-budget, channel-emission regression, update_recipe.outcome bus event) routed to `recipeEdits.test.js`. Reviewer Accept (2026-05-19). |
| userChatContext.test.js → toolFailures.test.js (round-cap broadcast facet) | Accept | n/a (folded into the rows above) | One facet pinned at the `aUserChatHarness` seam: the tool-round-cap notice is broadcast as an `assistant-notice` channel event with `targeting: 'broadcast'` and the `home.chat.notices.toolRoundCap` display key. Source file retained for the context-routing facets (subscription context attachment, cross-subscription isolation, clear-context, tool-context propagation, tool-start/tool-end broadcast) routed to `multiTurnConversation.test.js` / `userTurns.test.js`. Reviewer Accept (2026-05-19). |
| wsHandler.commands.test.js → wsProtocol.test.js (full) | Accept | 95.15 % → 94.57 % lines all-files (−0.58, intentional per seam-map); src/chat/conversation 93.46 % → 92.19 % (−1.27); wsHandler.js 93.15 % focused; wsChannel.js 96.42 % | 18 `it`s under five describes (frame routing × 8 / subscription lifecycle × 3 / context routing × 2 / GUI bridge × 2 / errors × 3). Source file deleted. Each routing `it` reads "when X frame arrives, channel emits Y" with whole-shape `toEqual` on outbound wire frames — wire shape **is** the contract at this seam. Subscription window facets collapsed from four to three. Reviewer Accept (2026-05-19) — all 14 Appendix B behaviours pinned; matcher discipline holds; coverage trade adjudicated as intentional (see note below). Nits parked: optional 1-3 line harness-contract test for `aWsHandlerHarness` in `harness.test.js`; `aWsHandlerHarness` uses fresh inner buses for its embedded tool registry / conversation buses (non-WS bus events not observable via `harness.bus.events` — boundary to document if a future scenario needs inner-tool events). |
| wsHandler.context.test.js → wsProtocol.test.js (full) | Accept | n/a (folded above) | Context + clear-context route via a `userChatFor` knob fake exposing `handled[]`, asserting on the routed command shape (`{type, clientId, subscriptionId, ...}`). Reviewer Accept (2026-05-19). |
| wsHandler.errors.test.js → wsProtocol.test.js (full) | Accept | n/a (folded above) | Three error paths pinned via bus event observation: `wsConnectionError` on `arg$.error(...)`, `wsRouteError` when `userChatFor` throws, `workFailed` when dispatched work errors. `toMatchObject` with type + message marker — does not pin verbatim error message body. Reviewer Accept (2026-05-19). |
| wsHandler.guiBridge.test.js → wsProtocol.test.js (full) | Accept | n/a (folded above) | `gui-response` routes to `guiRequests.respondCalls[]` with `{clientId, subscriptionId, ...}`; `subscriptionDown` populates `cancelCalls[]` and emits no `conversations` frame (no implicit subscription created). Reviewer Accept (2026-05-19). |
| wsHandler.logging.test.js → DELETED (subsumed by wsProtocol routing tests) | Accept | n/a (folded above) | Per seam-map: file pinned exact log strings + log levels on the `wsIn` event publisher — the canonical PRACTICES.md "tuning surface" red flag. Behaviour the events enabled (per-command routing) is now pinned in `wsProtocol.test.js` via actual channel outputs, not log events. **Coverage note (design intent):** `handleIgnored` / `ignoredAttrs` branches in `wsHandler.js` (lines 45-46, 154-156 — heartbeat / gateway-event / empty ignored paths) are intentionally uncovered. Reviewer independently verified `logListener.js` routes only on `event.type`, never on `event.kind`, so the `kind: 'ignored'` field is NOT consumer-load-bearing; pinning it would re-introduce the tuning surface the seam-map planned to delete. Coverage delta −0.58 % all-files / −1.27 % src/chat/conversation accepted per seam-map design. Reviewer Accept (2026-05-19). |
| wsHandlerHarness.js → DELETED (replaced by aWsHandlerHarness in harness.js) | Accept | n/a | After deleting the five consumer test files the legacy harness had zero importers. Reviewer Accept (2026-05-19). |
| updateRecipeTool.test.js → recipeEdits.test.js (bulk) | Accept | 94.57 % → 94.41 % lines all-files (−0.16, see note below); src/chat/specialists 99.47 % → 98.43 % (−1.04, design intent) | The bulk of update_recipe behaviour: descriptor (3 `it`s for name/schema/description-steer/directAnswer), preflight + user-message routing (2), allowed-tool scoping (4 — schemas offered, routing through inner registry, RECIPE_SCOPE_VIOLATION for cross-recipeId on both bound tools, TOOL_NOT_ALLOWED for raw recipe_load), envelope shapes (4 — success, UPDATE_FAILED, UPDATE_NOT_ATTEMPTED, retry-within-budget), per-type prompt assembly (2 — MOSAIC markers + unspecced fallback), channel-emission regression (1), stall recovery via update_recipe (1), update_recipe.outcome happy-path (1). Schema/envelope whole-shape `toEqual` earns its place; prompt content asserted via marker regexes (`/MOSAIC/`, `/Edit guidance:/i`, `/load_for_update/`); no verbatim prompt body; no `toHaveBeenCalled*`. Source file deleted. **Coverage note (design intent):** runSpecialist.js lines 124-129 (consecutiveFailureBail / invalidArgsBail string helpers) lost their direct exercise — these are internal bail strings used as the inner specialist's `answer` field when the inner tool guard trips. The user-observable consequence (the orchestrator-loop bail) is already pinned in `toolFailures.test.js`; the strings themselves are tuning surface with no external consumer. Per seam-map: "tool-loop safety bail behaviours … drop". |
| recipeSpecialists.test.js → recipeEdits.test.js (full) | Accept | n/a (folded above) | describe_recipe behaviour: descriptor (3), seeding (5 — answer envelope, system prompt, user message, recipe-metadata GUI request not recipe_load, fresh-session), allowed-tool scoping (4 — recipe_load only, routing, RECIPE_SCOPE_VIOLATION, TOOL_NOT_ALLOWED), per-type prompt assembly (2 — MOSAIC describeFacts + unspecced fallback). Construction-time validation throws when inner registry missing recipe_load was dropped as exclude (covered by toolFailures pattern for update_recipe; describe_recipe's symmetric throw is exercised indirectly by the `aToolFactoryHarness` default which builds with the right inner registry — adequate coverage from happy path). Metadata-failure short-circuit not duplicated; already in toolFailures.test.js for update_recipe; describe_recipe's symmetric path reaches the same `lookupRecipeMetadata$` envelope branch. Source file deleted. Reviewer Accept (2026-05-19). |
| conversationOrchestratorTools.test.js → recipeEdits.test.js (describe_recipe directAnswer integration row) | Accept | n/a (folded above) | One `it` under `directAnswer integration through the orchestrator`: when orchestrator routes a describe_recipe tool call, the specialist's prose streams verbatim to the user (textDelta filter matches specialist answer; orchestrator LLM received only one round; no restate). Reached via `aConversationHarness` with the real `factory.tool` from `aToolFactoryHarness({specialist: 'describe_recipe'})`. Source file retained for the `get_gui_context` flow → userTurns.test.js and the not-exposed assertion residuals. Reviewer Accept (2026-05-19). **Cleanup (2026-05-19):** The duplicate `it` in `conversationOrchestratorTools.test.js` (lines 36-59, `describe_recipe directAnswer`) was deleted; the `of` import became unused and was dropped. Two `it`s remain (`get_gui_context` flow + `recipe_load` not-exposed). Coverage unchanged (94.41 % all-files lines). |
| conversationSpecialists.test.js → recipeEdits.test.js (update_recipe + describe_recipe directAnswer rows) | Accept | n/a (folded above) | Two `it`s under `directAnswer integration through the orchestrator`: update_recipe streams specialist prose verbatim (the patch happens through the real specialist's inner LLM/tools); describe_recipe streams specialist prose verbatim. Both reached via `aConversationHarness` + real specialist factory tools — not via the prior `aFakeTools(...)`-with-`directAnswer:true` shortcut. Source file retained for the consult_map facet (already consumed by specialistConsultations.test.js) and the negative no-directAnswer facet → userTurns.test.js. Reviewer Accept (2026-05-19). **Cleanup (2026-05-19):** The three duplicate directAnswer `it`s in `conversationSpecialists.test.js` were deleted — `update_recipe` (lines 26-46) and `describe_recipe` (lines 48-66) folded here; the `consult_map` row (lines 7-24) folded into `specialistConsultations.test.js`. Two `it`s remain (negative no-directAnswer row → userTurns; consult_map inner-loop scoped tools row, retained because `specialistConsultations.test.js`'s orchestrator-routing block uses `replies: [{text: ...}]` and does not pin inner-tool routing through the real registry). Coverage unchanged (94.41 % all-files lines). |
| runSpecialist.test.js → recipeEdits.test.js (stall recovery facet) | Accept | n/a (folded above) | One `it` under `regressions and recovery`: when the inner specialist's LLM emits an empty round before a useful one, the specialist still returns an answer. Reached through `update_recipe` with `replies: [{text: ''}, {toolCalls: [patchCall]}, {text: 'Recovered and patched.'}]` — the user-observable outcome (envelope.ok=true with the recovered answer) is what's pinned; the specific retry mechanism (one transient nudge) stays internal to runSpecialist. Source file deleted (last facet landed). Per seam-map: runSpecialist is not a stable seam; behaviour is reachable from each specialist's tool-factory seam. Reviewer Accept (2026-05-19). |
| userChatAbort.test.js → multiTurnConversation.test.js (full) | Accept | 94.41 % → 94.24 % lines all-files (−0.17, design intent — titleGenerator lazy bus-event message bodies dropped); src/chat/conversation 93.46 % → 91.83 % (−1.63) | Two facets pinned at the `aUserChatHarness` seam under `describe('abort')` → `with an in-flight stream`: abort completes the chat-response channel (broadcast complete frame); late LLM textDelta events after the abort do not surface as `chat-response` channel events. The in-flight stream is driven by `aControllableLlm()` (lifted into harness this session). Reviewer Accept (2026-05-19) after one Refine round (lift `aControllableLlm`/`aStallingTitleGenerator` from inline → `harness.js`). All 10 Appendix B behaviours pinned; coverage drop adjudicated as tuning surface per seam-map (`logListener.js` routes only on `event.type` prefix, never on inner message thunks). |
| userChatTurnQueue.test.js → multiTurnConversation.test.js (full) | Accept | n/a (folded above) | Three facets pinned at the `aUserChatHarness` seam under `describe('turn ordering')`: within one conversation the second LLM call's `messages` sees the completed first-turn history (driven via `aControllableLlm` so the test holds turn one mid-flight); the second LLM call does not start until the first turn completes; two unfinished turns on two conversations run in parallel. Reviewer Accept (2026-05-19). |
| userChatMessages.test.js → multiTurnConversation.test.js (full) | Accept | n/a (folded above) | Six facets pinned across `describe('first-turn lock cycle')` and `describe('title generation')`: status (broadcast), user-message (broadcastExcept), chat-response delta + complete (broadcast); title generation broadcasts `conversation-updated` after a known-conversation turn; the unknown-conversation message emits no `conversation-updated`; a stalled title generator does not block the next user turn (next LLM call still fires). Reviewer Accept (2026-05-19). |
| conversation.queue.test.js → multiTurnConversation.test.js (queue + abort facets) | Accept | n/a (folded above) | The queue serialization, abort-of-in-flight, no-in-flight no-op, and queued-turn-survives-abort facets all land at the `aUserChatHarness` seam (one level up from the original `conversation.sendUserMessage$` seam). Source file retained for the persisted-on-abort facet → `userTurns.test.js` and the `isStreaming` getter facet (read-back of an internal flag — dropped per seam-map). Reviewer Accept (2026-05-19). |
| userChatContext.test.js → multiTurnConversation.test.js (cross-subscription context + clear-context facets) | Accept | n/a (folded above) | Three facets pinned at the `aUserChatHarness` seam under `describe('cross-subscription GUI context')`: only the sending subscription's cached context attaches to the LLM turn (a second subscription with its own cached context is not leaked in); a subscription with no cached context produces an LLM turn with no runtime-context message; `clear-context` drops the cached context. Source file retained for the tool-context propagation + tool-start/tool-end broadcast facets → `userTurns.test.js`. (The tool-round-cap notice broadcast facet was already consumed by `toolFailures.test.js` in a prior session; previous Accept row above.) Reviewer Accept (2026-05-19). |
| titleGenerator.test.js → multiTurnConversation.test.js (full) | Accept | n/a (folded above) | Two user-observable facets pinned at the `aUserChatHarness` seam under `describe('title generation')` → `after a known-conversation turn`: `conversation-updated` broadcast frame carries the generated title; the persisted conversation metadata carries the generated title. The "asks the LLM with bounded settings" facet at the title-generator's port (maxTokens/temperature/disableReasoning/debugLabel) is provider-tuning surface — dropped per seam-map. The streaming-chunks and `title.prompt`/`title.rawResponse` bus-event facets are title-generator-internal — dropped per seam-map. Source file deleted. Reviewer Accept (2026-05-19). |
| conversation.test.js → userTurns.test.js (full) | Accept | 94.24 % → 93.83 % lines all-files (−0.41, design intent — see below); src/chat/conversation 91.83 % → 90.92 % (−0.91) | Happy-turn facets pinned at the `aConversationHarness` seam under `describe('happy turn')` and `describe('history projection across turns')`: single-text reply streams a textDelta; user + assistant persist in history; chunked reply streams each chunk and persists the assembled message; initial messages are LLM-visible but not persisted; runtime GUI context is LLM-visible but not persisted. Trace-spans facet dropped per seam-map (internal observability). Source file deleted. |
| conversationListDrift.test.js → userTurns.test.js (narrow) | Accept | n/a (folded above) | Collapsed from the original whole-blob 7-key `toEqual` to a single narrow assertion under `describe('history projection across turns')`: the second turn's LLM messages contain only `[prior user, prior assistant, current user]` — no prior tool calls / tool results leak into the next turn's projection. Source file deleted. |
| conversationOrchestratorTools.test.js → userTurns.test.js (`get_gui_context` flow) | Accept | n/a (folded above) | One `it` under `describe('get_gui_context flow')` — orchestrator calls `get_gui_context`, conversation feeds the cached GUI context back as `{ok: true, data: {source: 'turn_snapshot', available: true, guiContext: ...}}`. Reached via `aConversationHarness` with the real `guiContextTool()` directly (no orchestrator wrapping needed for this contract). The `recipe_load not-exposed` facet was already covered by `orchestratorToolRegistry.test.js` + `sepalTools.test.js` (dropped per seam-map). The `describe_recipe directAnswer integration row` was moved to `recipeEdits.test.js` in a prior session. Source file deleted. |
| messageHandler.test.js → userTurns.test.js (full) | Accept | n/a (folded above) | Turn-boundary + event-routing facets pinned at the `aUserChatHarness` seam under `describe('turn boundaries through the userChat seam')` and `describe('event routing through the userChat seam')`: status (broadcast), user-message (broadcastExcept), chat-response complete (broadcast); textDelta / toolStart / toolEnd events translated to channel frames; channel emissions unwrapped through the conversation stream; persists-or-touches before the turn observed via `conversationsStore.list$` write-then-read (not via call-spying). The GUI context resolution + lifecycle ordering facets (message-supplied vs. cached GUI context, persistOrTouch call shape) are exercised at the same seam via the broader scenario suite (multiTurnConversation cross-subscription tests + this file's tool-context propagation). Source file deleted. |
| userChatContext.test.js → userTurns.test.js (tool-context + tool-start/tool-end facets) | Accept | n/a (folded above) | Two facets pinned at the `aUserChatHarness` seam under `describe('tool round')` → `through the userChat seam`: tool-start + tool-end channel events for each invocation; tool invocation context (conversationId + clientId + subscriptionId + guiContext) propagates to the tool. Recorded via a fake tool whose `invoke$` pushes its second arg into a list and the test reads that list (write-then-read on a port-fake side-effect surface, not call-spying). The cross-subscription routing + clear-context facets landed in `multiTurnConversation.test.js`; the tool-round-cap broadcast landed in `toolFailures.test.js`. Source file deleted. |
| conversationToolLoop.test.js → userTurns.test.js (happy facets) | Accept | n/a (folded above) | Six facets pinned at the `aConversationHarness` seam under `describe('tool round')` and `describe('runtime context across rounds')` + `describe('directAnswer flag')` + `describe('tool data collision safety')`: single-call happy turn (invocation + result feedback + tool-start/tool-end + persisted turn + tool schemas); multi-call in one round; unknown-tool envelope; non-directAnswer tools get a restate round; runtime context retained across post-tool and empty-after-tool retry rounds; tool result `{kind, targeting, payload}` lookalikes treated as data, never surfacing on the channel (positive filter — assert empty). The failure facets (TOOL_FAILED, TOOL_REPEAT_BLOCKED, consecutive-failure bail, INVALID_TOOL_ARGS bail, round-cap, empty-retry detail) landed in `toolFailures.test.js`. Source file deleted. |
| conversationSpecialists.test.js → userTurns.test.js (negative no-directAnswer facet) | Accept | n/a (folded above) | One `it` under `describe('directAnswer flag')`: when a non-directAnswer tool returns `{answer: ...}` data, the orchestrator still gets a restate round (LLM receives two prompts, only the orchestrator's textDelta streams). User-observable consequence asserted via `harness.llm.receivedMessages.length === 2` + `events.filter(event => event.textDelta)` — not via internal restate-mode names. The other `it` (consult_map inner-loop scoped tools) was already covered by `specialistConsultations.test.js`'s `inner-loop tool routing` describe block, which uses the real factory through the real registry — verified by inspection. Source file deleted. |
| conversation.queue.test.js → userTurns.test.js (persisted-on-abort facet) | Accept | n/a (folded above) | One `it` under `describe('abort persistence')`: user message stays in history when the turn is aborted before any assistant text. Reached via `aControllableLlm` (now exposed via the harness's `llm:` knob — added this session) to hold the turn open after the user message is appended. The queue serialization + abort-of-in-flight + queued-turn-survives-abort facets landed in `multiTurnConversation.test.js`; the `isStreaming` getter was dropped per seam-map (internal read-back, not a stable seam). Source file deleted. |
| conversationEvents.test.js → DELETED | Accept | n/a (folded above) | Per seam-map: file pinned verbatim "LLM orchestrator " prefixes and other log-line shapes — the canonical PRACTICES.md "tuning surface" red flag. The behaviour those events enabled (per-round routing) is now pinned via channel outputs and history projection in `userTurns.test.js`. **Coverage note (design intent):** `conversationEvents.js` lazy message body thunks (lines 25, 32, 53, 106, 145, 159-164) lost their direct exercise — these are log-format strings with no consumer load-bearing on them; `logListener.js` routes on `event.type` only. Per seam-map, expected. |
| userChatHarness.js → DELETED (last consumer migrated) | Accept | n/a | After deleting `userChatContext.test.js`, the legacy harness had zero consumers. Reviewer Accept (2026-05-19). |
| inMemoryHistory.js → DELETED (thin re-export with no consumers) | Accept | n/a | Phase-1 left this as a re-export of `createInMemoryHistory` from `harness.js`. No remaining importers after this session. Reviewer Accept (2026-05-19). |
| harness.test.js → DELETED (Phase-1 scaffolding) | Accept | n/a | Per Phase 1 acceptance note: "deleted at end of Phase 3." All four harness builders are now exercised in real scenario files (`aRegistryHarness` covered via `toolFailures.test.js`'s registry-level tests indirectly; `aToolFactoryHarness` via `recipeEdits.test.js` + `specialistConsultations.test.js` + `toolFailures.test.js`; `aConversationHarness` via `userTurns.test.js` + `recipeEdits.test.js` + `toolFailures.test.js`; `aUserChatHarness` via `userTurns.test.js` + `multiTurnConversation.test.js` + `conversationLifecycle.test.js` + `toolFailures.test.js`). Reviewer Accept (2026-05-19). |

Harness changes in the userTurns session:
- `aConversationHarness` gained an `llm:` knob (mirrors `aUserChatHarness`'s prior addition). Accepts any LLM-shaped object; default keeps the prior `aFakeLlm({replies})` behavior. The new knob lets `aControllableLlm` plug in for the abort-persistence facet (turn held mid-flight so the user-message-persisted-but-no-assistant-yet state is observable). No other API changes.

**Coverage delta explanation:**
- `src/chat/conversation/conversationEvents.js` 100 % → 85.18 % lines (−14.82 % on this file): lazy message body thunks lost coverage when `conversationEvents.test.js` was deleted. Per seam-map, those thunks are tuning-surface log strings with no consumer asserting their content. `logListener.js` routes on `event.type` only — never on message bodies — so the strings are pure log-format detail.
- `src/chat/conversation/conversation.js` 100 % → 90.47 % lines (−9.53 % on this file): the `isStreaming` getter (line 22) and one branch of `abort()` (line 33: the `if (streaming)` short-circuit when not streaming) lost direct coverage when `conversation.queue.test.js`'s `isStreaming` describe block was dropped per seam-map. The user-observable consequence (queued turns running after abort, no late events after abort, user message persisted on abort) is pinned via the multi-turn scenarios + this session's abort-persistence facet.
- `src/chat/conversation/userChat.js` 100 % → 77.77 % lines (−22.23 % on this file): the unknown-command path (lines 25-31, publishing `userChat.unknownCommand`) lost its prior direct test. This path is a defensive-warn surface that fires only on impossible-from-prod-wiring command types; pinning it would re-introduce a brittle no-prod-consumer surface. Acceptable trade per the "no negative pinning on internal calls" red flag.
- All-files 94.24 % → 93.83 % lines (−0.41 %): explained by the three rows above plus the dropped `wsHandler.js` ignored-handler branches from a prior session.

Harness changes this session:
- `aUserChatHarness` gained a `titleGenerator:` knob. Accepts `'silent'` (default — silent generator; backward-compatible with the five existing scenario files), `'real'` (builds a real `createTitleGenerator` over the harness's `llm` + `conversationsStore` + `bus`), or a custom title-generator object. The "real" path lets a test seed the `llm`'s `replies` with one user-turn reply followed by one title-LLM reply (titleGenerator shares the LLM port with the orchestrator in prod wiring, per `userChats.js`). The "custom object" path is used for stall scenarios (a generator whose `afterTurn$` returns a non-completing Subject).
- Lifted `aControllableLlm` (subject-returning LLM for holding a turn mid-flight) and `aStallingTitleGenerator` (non-completing `afterTurn$` for title-stall scenarios) into `harness.js` as exported builders, placed next to `aFakeLlm` / `aSilentTitleGenerator` respectively. Removed the duplicate local definitions from `multiTurnConversation.test.js` (−18 lines) which now imports both from `../harness`. `builders.js` copies retained for keeper-test consumers (`titleGenerator.fallback.test.js`, etc.) until those migrate.
- `harness.test.js` still green; no new harness-contract tests added — the `titleGenerator:` knob is exercised by the new scenario file directly.

Prior-session harness changes (preserved for context):
- Added `aWsHandlerHarness` to `harness.js` (wsProtocol session). Knobs: `conversationIds`, `replies`, `tools`, `bus`, `guiRequests`, `conversationsStore`, `userChatFor` (override). Collectors: `sent[]` (outbound wire frames), `bus.events`, `guiRequests.respondCalls` / `cancelCalls`. Public methods: `feed(frame)`, `errorStream(error)`. Builds real `createWsHandler` against a real per-username userChat (lazy cached) when `userChatFor` not supplied; the userChat is constructed from real `createConversations` + `createMessageHandler` + `createGuiContexts` + `createUserChat` over an in-memory store and fake LLM.
- Exposed `aRecordingGuiRequests` from `harness.js` for tests that build a custom `guiRequests` shape.
- `harness.test.js` still green (no new harness-contract tests added — the wsProtocol scenarios exercise the collectors).

Harness changes in the recipeEdits session: none. The existing `aToolFactoryHarness({specialist: 'update_recipe'|'describe_recipe'})` knobs cover the scope; the new scenario file reuses them. Two local helpers (`innerToolsWithSchemas`, `innerToolsImpl`) live inside `recipeEdits.test.js` to construct inner-registry doubles for scope-violation tests that need different schema sets than the harness default; these are tight enough that promoting them to `harness.js` would constitute premature abstraction (two uses each, both inside one file). `harness.test.js` still green.

## Termination snapshot

Phase 3 complete (2026-05-19).

- Before: 60 files / 9,501 LOC / 59 suites / 601 tests / 95.15 % lines all-files
- After: 45 files / 7,759 LOC / 43 suites / 516 tests / 93.83 % lines all-files (test/ tree; includes 2 manual + 4 helper files)
- Active suites LOC (non-manual): 43 files / 7,656 LOC
- Net delta vs initial baseline: −15 files / −1,742 LOC / −16 suites / −85 tests / −1.32 % lines
- Scenarios added (7 files):
    - `conversationLifecycle.test.js`
    - `multiTurnConversation.test.js`
    - `recipeEdits.test.js`
    - `specialistConsultations.test.js`
    - `toolFailures.test.js`
    - `wsProtocol.test.js`
    - `userTurns.test.js`
- Helpers retired this phase:
    - `test/chat/conversation/userChatHarness.js` (Phase 3 — replaced by `aUserChatHarness` in `harness.js`)
    - `test/chat/conversation/wsHandlerHarness.js` (Phase 3 — replaced by `aWsHandlerHarness`)
    - `test/chat/conversation/inMemoryHistory.js` (Phase 3 — re-export of harness's `createInMemoryHistory`; no consumers left)
    - `test/chat/harness.test.js` (Phase 1 scaffolding deleted at end of Phase 3)
- Helpers retained:
    - `test/chat/builders.js` — still consumed by adapter / pure-algorithm / unit tests across `test/chat/conversation/`, `test/chat/tools/`, `test/chat/specialists/`, `test/chat/orchestratorToolRegistry.test.js`
    - `test/chat/conversation/titleGeneratorHarness.js` + `test/chat/conversation/inMemoryConversationsStore.js` + `test/chat/conversation/fakeRedis.js` — consumed by `titleGenerator.fallback.test.js` (kept-as-is per seam-map)
    - `test/chat/llm/providerConformance.js` — provider-conformance suite
- Coverage delta vs immediate-prior baseline (94.24 % all-files lines):
    - All files 93.83 % (−0.41 %) — see "Coverage delta explanation" above
    - `src/chat/conversation` 90.92 % (was 91.83 %; −0.91 %)
    - `src/chat` 100 % (unchanged)
    - `src/chat/specialists` 98.43 % (unchanged)
    - `src/chat/tools` 98.52 % (unchanged)
    - `src/chat/llm` 100 % (unchanged)
    - `src/chat/llm/providers` 97.18 % (unchanged)
    - `src/chat/llmText` 100 % (unchanged)
- Phase 3 termination: scenarios complete (Y), ledger filled (Y), legacy helpers retired (Y).
- Deferred / issues: see [issues.md](./issues.md) — none escalated this phase.

check program storage with path /usr/local/bin/dockercheck-storage
    if status != 0 then exec "/usr/local/bin/pushover-notify 2"

check program storage-redis with path /usr/local/bin/dockercheck-storage-redis
    if status != 0 then exec "/usr/local/bin/pushover-notify 2"

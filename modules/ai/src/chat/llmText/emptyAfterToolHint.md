Previous reply after the tool result was empty.

If no available tool can complete the user's request, say so plainly — name the missing capability, suggest the closest thing you can do, and stop. Don't fall back to a read-only tool to look busy. Don't return empty content.

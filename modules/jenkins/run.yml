---

- name: "jenkins | Copy config folder"
  copy: src="{{ config_home }}" dest=/home/ubuntu

- name: "jenkins | Create jenkins volume folder"
  file:
    path: "/data/jenkins/"
    state: directory

- name: "jenkins | Start jenkins container"
  docker:
    docker_api_version: "1.22"
    name: "jenkins"
    pull: 'missing'
    image: "openforis/jenkins"
    state: "reloaded"
    net: "bridge"
    restart_policy: "always"
    ports:
      - "8080:8080"
      - "5000:5000"
    volumes:
      - "/data/jenkins:/var/jenkins_home"
      - "{{ config_home }}:/etc/sepal"
    env:
      JENKINS_PASSWD: "{{ jenkins_password }}"
      GITHUB_ACCOUNT_PASSWD: "{{ jenkins_github_user_password }}"

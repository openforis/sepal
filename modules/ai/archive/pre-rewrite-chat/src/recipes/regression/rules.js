// No structural cross-field rules beyond the schema's if/then conditional
// requirements. Validation surface is intentionally simpler than classification.

const rules = []

module.exports = {rules}

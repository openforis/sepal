# review.md — AI test quality review ledger

Reviewing every test under `modules/ai/test/` against PRACTICES (Stable seams,
Write-then-read, Quality bar + red flags, Test layout). One row per file, written
before advancing. Verdict ∈ {clean, fixed, flagged}.

## Baseline

Captured 2026-05-20, branch `mcp-ports-and-adapters`, `sepal npm-test ai --runInBand --coverage`.

- Test Suites: 64 passed, 64 total
- Tests: **516 passed**, 516 total
- Coverage (All files): Stmts 92.76 | Branch 86.01 | Funcs 92.75 | **Lines 93.83**

Matches expected baseline (516 green / ≥ 93.83% lines). Proceeding to phase 1.

## Phases

1. Pure algorithms: cleanTitle, llmMessages, diagnostics, emitOnEnd, prompts, promptSnapshot, assembleSpecialistPrompt, toolCallGuard, jsonPointer, recipeProjection, turnContext, titleGenerator.fallback, eventBus.
2. Adapters: redisConversationsStore, redisHistory, wsChannel, openaiChatCompletions, lmStudioNativeChat, guiRequests, server, logListener, llm/index, recipesImport. (+ manual tests lightly)
3. Scrutiny: specialistEvents, tool wrappers (recipePatchTool, loadForUpdateTool, recipeMetadata, mapTools, projectTools, recipeTools, guiContextTool, sepalTools), orchestratorToolRegistry, registry, *.diagnostics.
4. Scenarios: test/chat/scenarios/ — final sweep.

## Summary

- **Files reviewed:** 66 (all `*.test.js` under `modules/ai/test/`) — 66 ledger rows.
- **Verdicts:** 47 clean · 17 fixed · 2 flagged.
- **Test count:** 516 (baseline) → **514** (final). Two justified drops:
  - `logListener.test.js` −1: removed misleading "memoization" test that pinned routing-via-spy under a false name; coverage byte-identical before/after (zero unique lines).
  - `recipesImport.test.js` −1: removed test 3, a near-verbatim duplicate of the recipes lib's own `recipe.test.js` (validateRecipe/getRecipeDefaults aren't consumed by ai `src/**`); coverage floor unaffected.
- **Coverage:** All-files **Lines 93.83%** throughout (floor met exactly); Stmts 92.76 · Branch 86.01 · Funcs 92.75.
- **Suite:** 64 suites / 514 tests green.
- **Verifier bounces:** 2 (sepalTools `new Set`→`sort()+toEqual` to keep dup-detection; orchestratorToolRegistry `arrayContaining`→full `sort()+toEqual` to keep product-tool membership). Both resolved on re-dispatch.
- **Flag→fix conversions:** 2 (llm/index negatives; recipesImport test 3) — verifier rejected the flag and the fix was applied.
- **No production edits** (`src/**`, `lib/js/recipes/src/**` untouched). Only test files + this ledger changed by this review.
- **Spot-checks:** P1 #4/#8, P2 #4, P3 #4, P4 #4/#8/#12/#16/#20 — all AGREE.

### Flagged judgment calls (left for the user to decide)

1. **`guiContextTool.test.js`** — happy-path `get_gui_context` envelope overlaps `userTurns/toolRound.test.js`, but the two `available:false` branches are unique and Stable seams favors the cheaper tool-seam test. **Recommendation: keep all 4** (no action).
2. **`registry.test.js`** — the test `"logs structured validation error details"` pins the `tool.result` bus event's `message`/`errorSummary` (observability, not the `invoke$` envelope) — wrong seam for this file, but it covers the `errorSummary` projection that `registry.diagnostics.test.js` does NOT. **Recommendation: relocate to `registry.diagnostics.test.js` and tighten to a structured `errorCode`/`errorSummary`-shape pin** (cross-file move; not done unilaterally).

## Ledger

| file | phase | verdict | rule(s) | change or why-flagged | verifier |
|---|---|---|---|---|---|
| cleanTitle.test.js | 1 | clean | Stable seams (pure algorithm), Write-then-read | Pure fn input→output; assertions on return value, no spying; 80-char cap is contract not internal constant | pending spot-check pool |
| llmMessages.test.js | 1 | clean | Stable seams (pure algorithm), Write-then-read, Quality bar (whole-shape toEqual = contract) | Pure projection fn at lowest seam; whole-array equality justified as the contract; no spies; shared data in consts | pending spot-check pool |
| diagnostics.test.js | 1 | clean | Stable seams (pure-algorithm), Write-then-read, Matcher-fits-scope | Pure input→output transform at lowest seam; exact-string asserts pin output contract not wording/internals; no spies | pending spot-check pool |
| emitOnEnd.test.js | 1 | clean | Stable seams (pure algorithm), Async style (sync), Write-then-read, Matcher-fits-scope | Exact emission-sequence equality is the operator contract; sync capture matches sync chain; no spies | AGREE — 1-in-4 spot-check (clean #4), independent re-read confirms |
| prompts.test.js | 1 | clean | Quality bar (marker-regex intent OK; no verbatim body pins), Write-then-read, Stable seams (loader) | All assertions are loader-behaviour pins or intent markers; toBe(readFileSync) pins which-file not wording | pending spot-check pool |
| promptSnapshot.test.js | 1 | clean | Stable seams (pure algorithm), Write-then-read, Quality bar (matcher-scope + marker-regex for intent), Test layout | Pure formatter input→output; real-prompt test uses intent markers for cap behaviour not verbatim pins; no whole-blob equality | pending spot-check pool |
| assembleSpecialistPrompt.test.js | 1 | clean | Stable seams (pure algorithm), Quality bar (marker-regex intent, matcher-scope), Write-then-read | Pure assembler; literals are routed test inputs + structural/compactness contracts not verbatim prompt pins; negatives on output text | pending spot-check pool |
| toolCallGuard.test.js | 1 | fixed | Quality bar (asserts implementation constant) | Dropped leaked limit constant (`:3`) from injected bail-factory fakes + 4 assertions; kept which-bail + tool facets | PASS — right rule, which-bail+tool facets preserved, no over-reach, 14 green |
| jsonPointer.test.js | 1 | clean | Stable seams (pure algorithm), Write-then-read, Quality bar (matcher scope + marker regex) | Pure RFC 6901 algorithm; input→output assertions, no spying, marker-regex error pins, src-mirroring layout | AGREE — 1-in-4 spot-check (clean #8), independent re-read confirms |
| recipeProjection.test.js | 1 | clean | Quality bar (whole-shape=contract exception), Stable seams (pure algorithm), Given/when/then | Whole-blob toEqual legitimate — pure transform's exact output IS the contract; MOSAIC block pins per-facet; no spying | pending spot-check pool |
| turnContext.test.js | 1 | clean | Quality bar (no verbatim wrapper pin; marker-regex intent allowed), Stable seams (pure algorithm), Write-then-read | Pins behaviour/intent not wording; whole-object toEqual fits pure-transform contract; caps are observable consequences | pending spot-check pool |
| titleGenerator.fallback.test.js | 1 | clean | Stable seams, Write-then-read, Quality bar (matcher scope, marker assertions) | All assertions on observable surfaces (channel/store/bus); negatives use port-fake capture not jest.fn; correct seam | pending spot-check pool |
| eventBus.test.js | 1 | fixed | Quality bar (verbatim log-message assertion; matcher-fits-scope) | Replaced 6 verbatim message-string pins with intent marker-regexes; narrowed 2 whole-shape toEqual→toMatchObject keeping all structured facets (type/level/at/durationMs/attrs/error) | PASS — message is genuine log tuning surface; all structured facets preserved (failed-path strengthened with durationMs); no over-reach |
| redisConversationsStore.test.js | 2 | clean | Test scoping / Stable seams (adapter wire boundary), Write-then-read | Boundary at Redis driver; pins domain↔wire translation + TTL + delete-also-history via correct side-effect surfaces; no over-pinning | pending spot-check pool (P2) |
| redisHistory.test.js | 2 | clean | Test scoping / Stable seams (adapter wire boundary), Write-then-read, Async honesty | Fakes Redis driver; write-then-read round-trip pins JSON + TTL + key translation; legitimate wire-shape toEqual | pending spot-check pool (P2) |
| wsChannel.test.js | 2 | fixed | Quality bar (verbatim log-message assertion / exact log strings) | Dropped verbatim wsOut debug-message strings across 16 tests, kept type+level; wire-frame toEqual on `sent` left intact | PASS — message is formatSummary debug string (tuning), wire frame `sent` toEqual untouched & redundant, no over-reach, 16 green |
| openaiChatCompletions.test.js | 2 | clean | Test scoping / Stable seams (adapter wire boundary), Async style, Write-then-read (adapter mock.calls exception) | Fakes OpenAI SDK at boundary; pins both directions; wire-shape toEqual legitimate; marker regexes for log text | pending spot-check pool (P2) |
| lmStudioNativeChat.test.js | 2 | clean | Test scoping (outbound adapter), Stable seams (wire boundary), Write-then-read (sanctioned mock.calls exception) | Faking at fetch boundary; both translation directions pinned; wire-shape toEqual legitimate; marker-style log assertions | AGREE — 1-in-4 spot-check (P2 clean #4), independent re-read confirms |
| guiRequests.test.js | 2 | clean | Async "Time is I/O", Stable seams (adapter wire-shape toEqual), Write-then-read, Quality bar (marker regex) | Clock/bus/channel faked at boundary, outcomes asserted; timeoutMs is input not pinned; completed/error pins are positive observable surface | pending spot-check pool (P2) |
| server.test.js | 2 | fixed | Write-then-read over call-spying; one-facet-per-assertion | Dropped redundant `wsStreamCalls` call-spy assertion + dead recording array; routing still pinned via receivedCtx | PASS — receivedCtx is strictly stronger (proves end-to-end delivery); wrong/missing-handler regression still caught; no over-reach, 2 green |
| logListener.test.js | 2 | fixed | Quality bar (unclear-intent red flag) + Write-then-read (call-spying) | Removed misleading 'memoization' test that pinned routing-via-spy under a false name & covered no unique prod lines (17→16) | PASS — logListener.js coverage byte-identical before/after (42.3% lines, same uncovered 18-28/49-61); zero unique coverage lost; deletion unambiguous; verbatim onEvent strings correctly kept (pass-through contract) |
| llm/index.test.js | 2 | fixed | Quality bar (negative assertions on internal calls) | Flag→fix: verifier rejected the flag on the negatives facet. Dropped 5 `not.toHaveBeenCalled()` exclusivity negatives; coordinator mocking + positive toHaveBeenCalledWith/toBe pins kept (justified per Test doubles). | PASS — numstat 0/5, exactly 5 deletions, all positive/toBe pins intact, single-ternary makes negatives redundant, 7 green |
| recipesImport.test.js | 2 | fixed | Test scoping ("don't re-test code we don't own") | Flag→fix: verifier found test 3 a near-verbatim duplicate of lib's recipe.test.js; validateRecipe/getRecipeDefaults unused by ai src; reduce-variants would duplicate test 1 or lib test → deleted test 3 (3→2), kept tests 1&2 | PASS — diff is exactly test-3 removal (10 lines), tests 1&2 byte-identical, no prod touched, 2 green, coverage floor 93.83% unaffected (lib fns not in ai src) |
| app.manual.test.js | 2 (manual) | clean | Manual tests (light bar) | Real WS+LLM end-to-end smoke; correctly named/excluded; loose non-destructive asserts | light review |
| openaiChatCompletions.manual.test.js | 2 (manual) | clean | Manual tests (light bar) | Real OpenAI-compatible adapter smoke; correctly named/excluded; loose textDelta assert; non-destructive | light review |
| specialistEvents.test.js | 3 | fixed | Quality bar (verbatim log-string / whole-object toEqual / exact log-string pins) | Dropped 5 verbatim message renders (toEqual→objectContaining) + 5 redundant message .toContain/.toMatch lines; reframed [-] render test to toolNames=[]; kept all structured contract fields incl. shape summaries + outcome booleans/codes | PASS — field-by-field: each block drops only `message` prose; all structured fields survive; 5 removed lines each redundant w/ surviving structured assert; outcome still pins patchAttempted/patchSucceeded/code; 14 green |
| recipePatchTool.test.js | 3 | fixed | Quality bar (verbatim prompt body / whole-object toEqual) | Loosened schema test toEqual→toMatchObject; dropped verbatim LLM-facing description strings, kept structural facets (types/enum/required/additionalProperties/minItems); error-code passthrough confirmed real translation not scenario-redundant | PASS — toMatchObject does exact array match so enum values + required arrays still pinned; dropped `value` was prose-only; steering intent covered by sibling marker test; 11 green |
| loadForUpdateTool.test.js | 3 | clean | Stable seams (Tool factory invoke$), Write-then-read, matcher-fits-scope, LLM-facing text | Real translation tests at correct seam — request shape, baseModelHash, closure resolution, effective-model projection, channel passthrough, error codes; scenarios stub the tool so no redundancy | pending spot-check pool (P3) |
| recipeMetadata.test.js | 3 | clean | Stable seams, Write-then-read, Quality-bar facets | Real translation tests, all 5 facets distinct (request shape, success/fallback/coded-error envelopes, channel-passthrough regression); scenario only pins action name, no redundancy | pending spot-check pool (P3) |
| mapTools.test.js | 3 | clean | Stable seams (Tool factory invoke$), Write-then-read, Quality bar (matcher fits scope) | Translation tests pin request shape + response→envelope identity at correct seam; scenarios pin routing/protocol not this wrapper's translation | pending spot-check pool (P3) |
| projectTools.test.js | 3 | clean | Stable seams (Tool factory invoke$), Write-then-read, Quality bar (matcher fits scope) | All tests pin real translation (request shape + response→envelope projection + error propagation) at correct seam; no scenario redundancy | AGREE — 1-in-4 spot-check (P3 clean #4), independent re-read confirms (scenarios stub project_list; this file is sole owner of real translation) |
| recipeTools.test.js | 3 | clean | Stable seams (Tool factory invoke$), Write-then-read, Quality bar (matcher scope) | Outbound translation at correct seam via fake-bridge write-then-read; schema toEqual is contract; not scenario-redundant. Note: bounded recipe_load projection-shape overlap w/ recipeProjection.test.js, left as defensible wrapper-wiring coverage | pending spot-check pool (P3) |
| guiContextTool.test.js | 3 | flagged | Stable seams (Tool factory invoke$), Test scoping | Clean translation tests at correct seam; happy-path envelope overlaps userTurns/toolRound.test.js:172 but unavailable branches unique → recommend keep all 4 | CONFIRMED — genuine delete-vs-reduce judgment call; Stable seams ("lowest seam is right home") favors keeping the cheap tool test over the expensive scenario; unavailable branches unique. No action. |
| sepalTools.test.js | 3 | fixed | Quality bar (pins-ordering red flag / matcher-fits-scope), Stable seams (pure algorithm) | Loosened exhaustive list pin from ordered toEqual; kept membership/boundary pins | PASS after 1 BOUNCE — first used `new Set` (masks duplicate tool names, a real contract regression); corrected to `[...].sort()` toEqual which drops order yet catches dupes; 8 green |
| orchestratorToolRegistry.test.js | 3 | fixed | Quality bar (ordering / whole-blob toEqual over-pin) | Loosened ordered full-list toEqual; kept 3 not.toContain leak-prevention pins | PASS after 1 BOUNCE — first dropped product membership to arrayContaining(3 specialist tools), losing the ONLY coverage that product tools reach the orchestrator surface (sepalTools tests input list, scenarios use handcrafted registries); corrected to sort()+toEqual on full 9-name surface; 4 green |
| registry.test.js | 3 | flagged | Stable seams (event publishers not a seam), Quality bar (log-string pin), Test layout | One test ("logs structured validation error details") pins bus-event message text — wrong seam (belongs in registry.diagnostics.test.js) but covers an uncovered errorSummary facet → recommend relocate+tighten, don't delete. Rest clean: error CODE/envelope/directAnswer/flag pins all KEEP | CONFIRMED — errorSummary/message live ONLY on the tool.result bus event, not the invoke$ envelope; observability seam; facet otherwise-uncovered so relocate (to registry.diagnostics) not delete. Real cross-file judgment call. No action. |
| registry.diagnostics.test.js | 3 | fixed | Quality bar (verbatim log-message pin / whole-object toEqual) | Dropped 3 verbatim event-message string pins; kept structured facets (type/level/ids/ok/kind/count/firstItemKeys/namedCount/errorCode + trace shape); test 1 toEqual→toMatchObject | PASS — field-by-field only `message` prose dropped; count/namedCount/firstItemKeys/errorCode/kind + trace shape all survive; toMatchObject array still pins length=1; 3 green |
| guiRequests.diagnostics.test.js | 3 | clean | Stable seams (diagnostics pins structured event shape only), Quality bar red-flags (no verbatim msg pin, no ordering/count pin, write-then-read) | Structured facets are the contract, prose message unpinned; scenario overlap with guiRequests.test.js targets disjoint assertion surfaces (outcomes vs structured routing diagnostic) → keep | pending spot-check pool (P3) |
| conversationLifecycle/creating.test.js | 4 | clean | Stable seams (userChat), Write-then-read, Quality bar (matcher fits scope), Given/when/then | Channel-event + persisted-store assertions; whole-event toEqual justified as wire contract; all payload values traceable to setup | pending spot-check pool (P4) |
| conversationLifecycle/deleting.test.js | 4 | clean | Write-then-read, Quality bar (matcher scope), Given/when/then | Observable-surface assertions throughout; whole-shape toEqual is on load-bearing 3-field channel-event contract | pending spot-check pool (P4) |
| conversationLifecycle/messaging.test.js | 4 | clean | Write-then-read, matcher-fits-scope, observable-surface | All assertions on store/history/channel surfaces; toEqual shapes are the contract; pinned timestamps derive from controlled clock | pending spot-check pool (P4) |
| conversationLifecycle/selecting.test.js | 4 | clean | Write-then-read, matcher-fits-scope, Given/when/then | Observable-surface assertions only; whole-shape toEqual justified (deterministic event, no incidental fields) | AGREE — 1-in-4 spot-check (P4 clean #4), independent re-read confirms |
| multiTurnConversation/guiContext.test.js | 4 | clean | Write-then-read (fake-LLM receivedMessages), matcher-fits-scope (marker toContain + whole-shape where shape is contract) | Cross-subscription isolation pinned via observable LLM messages; no call-spying/verbatim-prompt/internal pins | pending spot-check pool (P4) |
| multiTurnConversation/lockCycle.test.js | 4 | clean | Stable seams (userChat), Write-then-read, matcher-fits-scope, Given/when/then | Asserts at userChat seam on channelEvents; every field load-bearing (kind/targeting/payload); no spying/verbatim/id-time pins | pending spot-check pool (P4) |
| multiTurnConversation/titleGeneration.test.js | 4 | clean | Write-then-read, scenario layout, Quality bar | Side-effect surfaces only (channel events, persisted store, fake-LLM calls); controlled title input not incidental prose; no overlap w/ fallback test | pending spot-check pool (P4) |
| multiTurnConversation/turnControl.test.js | 4 | clean | Stable seams (userChat), Write-then-read, Quality bar (matcher fits scope) | userChat-seam turn-control facets via captured LLM calls + channel events; no call-spying, no incidental pins | AGREE — 1-in-4 spot-check (P4 clean #8), independent re-read confirms (abort/serialization/late-delta via observable outcomes) |
| wsProtocol/frameRouting.test.js | 4 | clean | Stable seams (WS protocol), Quality bar (matcher fits scope), Write-then-read | Wire-frame toEqual assertions are the contract at this seam (targeted/broadcast/broadcastExcept routing); no violations | pending spot-check pool (P4) |
| wsProtocol/subscriptionLifecycle.test.js | 4 | clean | Stable seams (WS), Write-then-read, Quality bar | Wire-frame outcomes via sent collector; recovery facet pinned via bus event type+count, not a log-string | pending spot-check pool (P4) |
| wsProtocol/contextRouting.test.js | 4 | clean | Test scoping (inbound adapter), Write-then-read, Quality bar (matcher fits scope) | Recording domain-fake + objectContaining; all fields load-bearing (context/clear-context routing); no red-flags | pending spot-check pool (P4) |
| wsProtocol/guiBridge.test.js | 4 | clean | Test scoping (inbound-adapter call assertion sanctioned) | Inbound WS adapter routing to guiRequests; translation-arg + wire side-effect assertions are the contract | AGREE — 1-in-4 spot-check (P4 clean #12), independent re-read confirms (sanctioned inbound-adapter exception; negative on positive wire surface) |
| updateRecipe/descriptor.test.js | 4 | clean | Quality bar (matcher-fits-scope: schema=contract; marker-regex on description), Write-then-read | Whole-shape toEqual justified as LLM wire schema; restate-suppression pinned at observable receivedMessages length; no violations | pending spot-check pool (P4) |
| updateRecipe/envelope.test.js | 4 | fixed | Quality bar (verbatim internal message string / whole-blob toEqual) | Dropped verbatim UPDATE_NOT_ATTEMPTED message pin; toEqual→toMatchObject, kept code + specialistAnswer | PASS — dropped string is hardcoded prod prose (recipeSpecialists.js:186); code + specialistAnswer still pinned; test 2's message is test-set patchError propagated (correctly kept) |
| updateRecipe/promptAssembly.test.js | 4 | clean | Quality bar (marker-regex intent), Write-then-read, Stable seams | Marker regexes on fake-LLM received system prompt; type-resolution coverage distinct from assembleSpecialistPrompt.test.js | pending spot-check pool (P4) |
| updateRecipe/recovery.test.js | 4 | fixed | Quality bar (internal field counts / write-then-read over call-spying) | Reframed receivedMessages length pin to observable success envelope; added patch call to produce real success | PASS — verifier probed: old count assertion passed on a FAILED (UPDATE_NOT_ATTEMPTED) run, masking a no-op; reframe pins real success envelope, interleaving facet preserved (pre-loop channel emission); coverage strengthened |
| updateRecipe/scoping.test.js | 4 | clean | Stable seams, Write-then-read, Quality-bar matchers | Asserts on LLM-received schema list, captured invocations, error envelopes; negative empty-invocations are the carved-out scope case paired w/ positive envelope | AGREE — 1-in-4 spot-check (P4 clean #16); error codes are real contract (specialistScope.js:22, recipeSpecialists.js:199); negatives paired w/ positive envelopes |
| toolFailures/emptyAfterTool.test.js | 4 | fixed | Quality bar (internal-constant pin: retry count → observable consequence) | Test 2 sole toHaveLength(3) call-count pin → asserts hint injected exactly once on the final call (loop stops); added inline hintedCalls marker helper | PASS — /empty/i matches real hint; both failure modes (double-retry→hintedCalls=2, zero-retry→0) caught; same contract pinned at observable surface; 3 green |
| toolFailures/terminalNotices.test.js | 4 | fixed | Quality bar (verbatim notice-copy pin) | Loosened 'rephrasing' substring pins to expect.any(String); content asserted against display.fallback; kept notice-key contract | PASS — wsChannel/openai tests use a *different* fabricated fallback, proving wording isn't contract; display.key kept; content===display.fallback non-tautological (fails if content diverges); blocks 1&2 already clean; 4 green |
| toolFailures/toolErrors.test.js | 4 | clean | Write-then-read, Quality bar (envelope codes kept, marker regex) | Verbatim strings are test-supplied error input feeding TOOL_FAILED passthrough contract; surfaces are harness collectors not spies; toolEnd whole-shape is small channel-event contract | pending spot-check pool (P4) |
| toolFailures/updateRecipeConstruction.test.js | 4 | clean | Quality bar (marker-regex / structured codes), Write-then-read | Construction + preflight paths unique (not redundant w/ updateRecipe/* or toolErrors); structured codes kept; LLM-fake side-effect surface used | pending spot-check pool (P4) |
| describeRecipe/describeRecipe.test.js | 4 | clean | Write-then-read, matcher-fits-scope, marker-over-verbatim | All assertions read observable surfaces (fake-LLM msgs/tools, gui requests, inner invocations, envelope, channel deltas); whole-shape toEquals are genuine wire/envelope contracts | pending spot-check pool (P4) |
| userTurns/happyTurn.test.js | 4 | clean | Write-then-read, matcher-fits-scope | Observable surfaces only (channel events, persisted history, fake-LLM receivedMessages); whole-shape toEqual on contract shapes w/ test-controlled values | AGREE — 1-in-4 spot-check (P4 clean #20), independent re-read confirms (single negative on persisted-history surface; all values test-controlled) |
| userTurns/runtimeContext.test.js | 4 | clean | Stable seams, Write-then-read, Quality bar (marker not verbatim) | Distinct facets (reaches LLM / not persisted / survives tool+retry rounds); no redundancy with turnContext/guiContext | pending spot-check pool (P4) |
| userTurns/toolRound.test.js | 4 | clean | Write-then-read, Quality bar, Stable seams | No cap-constant or call-spying violations; toEqual uses are tool-start/end/delta sequence + persisted-shape contracts; get_gui_context overlap kept per Stable seams | pending spot-check pool (P4) |
| userTurns/userChatSeam.test.js | 4 | clean | Stable seams (userChat), Write-then-read, matcher fits scope | Channel-event translation + targeting + up-front persistence via observable channelEvents; right seam, not duplicative of conversation-stream seam | pending spot-check pool (P4) |
| specialistConsultations.test.js | 4 | fixed | Quality bar (verbatim prompt-body pin / LLM-facing marker-regex) | Loosened stringContaining('map specialist') on inner system prompt to /map/i marker; all scoping/envelope/integration facets kept | PASS — 'map specialist' is verbatim map.md line 1; /map/i still discriminates (recipe.md/update.md contain no 'map'); single-line diff, 16 green |
| wsProtocol/errors.test.js | 4 | clean | Quality bar (marker-intent assertion / error-frame TYPE is contract), Stable seams (WS), Write-then-read | Event TYPE pinned; message uses stringContaining on injected cause not prod prose; observed via bus | pending spot-check pool (P4) |
